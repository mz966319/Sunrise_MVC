@model UploadViewModel


<div class="card container">
    <div class="row pt-4 pb-3">
        <div class="row text-center">
            <h2 class="text-primary">File Reader</h2>
        </div>

        

        <ul class="nav nav-tabs" role="tablist">
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle @((Model.ActiveTab == "#addNewStudents") ? "active" : "")"
                   data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Students</a>
                <div class="dropdown-menu">
                    <a class="dropdown-item @((Model.ActiveTab == "#addNewStudents") ? "active" : "")"
                       data-bs-toggle="tab" href="#addNewStudents">Add New Students</a>
                    <a class="dropdown-item @((Model.ActiveTab == "#dropdown2") ? "active" : "")"
                       data-bs-toggle="tab" href="#dropdown2">Another action</a>
                </div>
            </li>
        </ul>
        <div id="myTabContent" class="tab-content">
            <div class="tab-pane fade @((Model.ActiveTab == "#addNewStudents") ? "show active" : "")" id="addNewStudents">
                <form method="post" enctype="multipart/form-data" asp-action="UploadExcel" asp-controller="FilesReader">
                    <input type="hidden" name="activeTab" id="activeTab" value="#addNewStudents" />
                    <div>
                        <label for="formFile" class="form-label mt-4">Upload Excel File</label>
                        <input class="form-control" type="file" id="formFile" name="file" accept=".xlsx">
                        @if (!string.IsNullOrEmpty(Model.UploadedFileName))
                        {
                            <p class="mt-2">Uploaded File: <strong>@Model.UploadedFileName</strong></p>
                        }
                    </div>

                    <div>
                        <button type="submit" class="btn btn-primary mt-3">Upload</button>
                    </div>

                    <div>
                        <label for="exampleSelect2" class="form-label mt-4">Students List</label>
                        <select multiple class="form-select" id="exampleSelect2" style="height: 400px; overflow-x: auto;">
                            
                                @foreach (Student student in Model.ToAddStudentsList)
                                {
                                    <option>@student</option>
                                }
                        </select>
                    </div>
                </form>
            </div>




            <div class="tab-pane fade" id="dropdown2">
                <p>Trust fund seitan letterpress, keytar raw denim keffiyeh etsy art party before they sold out master cleanse gluten-free squid scenester freegan cosby sweater. Fanny pack portland seitan DIY, art party locavore wolf cliche high life echo park Austin. Cred vinyl keffiyeh DIY salvia PBR, banh mi before they sold out farm-to-table VHS viral locavore cosby sweater.</p>
            </div>
        </div>


    </div>

 
</div>

@section Scripts {
    @{
        <partial name="_ValidationScriptsPartial" />
    }
}